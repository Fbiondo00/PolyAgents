# DREAMS — PolyAgents dreaming review queue

Human-readable Dream Diary written by OpenClaw's dreaming pass (when enabled).
Skim periodically; reject what's clearly wrong, let good promotions land in
`MEMORY.md`. This is *not* a promotion source itself — it's the review surface.

Dreaming is configured under `plugins.entries.memory-core.config.dreaming` and
piggybacks on the heartbeat (heartbeat must be on, else dreaming reports
`blocked`). Toggle: `/dreaming on|off|status`.

_(No dream entries yet — dreaming will append here as it consolidates daily
notes.)_
