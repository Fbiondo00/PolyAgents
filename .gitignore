# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.snowflake/
.v0-trash/
.vercel/
next.user-config.*

# Environment variables (keep .env.example committed)
.env
.env.*
*.env
!.env.example

# Common ignores
node_modules
.next/
.DS_Store
tsconfig.tsbuildinfo

# Server-side vault registry (runtime data)
.vault-registry.json

# Runtime engine state / cached credentials
.engine-store.json

# Local Claude Code settings (per-machine permissions, may hold local paths)
.claude/settings.local.json

# MCP config (contains secrets — DB passwords — use .mcp.json.example as template)
.mcp.json

# Rust build artifacts
target/

# Terraform provider/plugin cache
.terraform/
.terraform.lock.hcl

# Package build artifacts
srcs/requirements/*/dist/
srcs/requirements/*/node_modules/
